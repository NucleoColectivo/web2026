'use client';

import React from 'react';

/**
 * Vista eliminada por solicitud del usuario.
 * Se mantiene el archivo vacío para evitar errores de importación si existen referencias residuales.
 */
export function MedularView() {
  return null;
}